// Unit testing for node server

var expect = require('chai').expect;

describe('CLIENT -- App', function() {
   it('should load the app into the frontend', function() {
   expect(true).to.be.ok;
    });
});

